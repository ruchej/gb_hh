[tool.poetry]
name = "gb_hh"
version = "0.1.0"
description = ""
authors = ["Aleksandr <lvar-8@ya.ru>"]

[tool.poetry.dependencies]
python = "^3.8"
Django = "^4.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
